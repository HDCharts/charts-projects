interface:
  display_name: "HDCharts Release Compatibility"
  short_description: "Generate HDCharts release migration notes"
  default_prompt: "Use $hdc-rc to check API compatibility and update the snapshot migration notes."
